# R1 HANDOFF — for a session scoped to `trading-src`

**Experiment:** R1 — Technical-Analysis Filter vs ATR as Signal Gate (pre-registered April spec).
**Handoff written:** 2026-07-02, from a session scoped to `stock-tracker` (wrong repo — see History).
**How to use:** start a new Claude Code session **on the private repo `trading-src`** and paste this whole file as the task.

## Step 0 — ground rules

- The pre-registered April method is authoritative. Do NOT re-derive, improve, or reinterpret it. One variable only.
- Verify first, act second. If verification fails, record the failure and STOP.

## Step 1 — VERIFY FIRST (read-only)

In the **current** `swing_trader.py` in `trading-src`, confirm:

1. The ATR signal gate still exists (the incumbent gate).
2. The technical-analysis ("tech-anal") gate still exists (the challenger).
3. They are swappable as the April spec assumes — same interface / call site, so a genuine one-variable swap is possible.

Record the verify result in the findings file BEFORE anything else. If the code has drifted (gate renamed, removed, interface changed), record a spec-drift finding tagged `[Proven]` and stop — do not silently adapt the method.

## Step 2 — only if Step 1 passes

Run the experiment exactly as pre-registered:

- Replace the ATR gate with the tech-anal gate. Everything else held constant.
- Accept iff **BOTH**: win rate improves by ≥3 percentage points **AND** viable signal count stays at or above the threshold defined in the April spec. If the spec's threshold value can't be located, that is a blocker to record — not a judgment call to make.
- Tag all findings `Proven` / `Suspected` / `Rejected`.

## Output

Append (never overwrite) to `tech_anal_vs_atr_findings.md`.

- Canonical copy as of 2026-07-02: repo `stock-tracker`, branch `claude/intelligent-curie-j1hsfo` — contains two verify-first records dated 2026-07-02, both FAILED because those sessions were scoped to the wrong repo.
- A `trading-src`-scoped session cannot read `stock-tracker`. If `trading-src` has no copy of the findings file, create one there, note that records 1–2 live in `stock-tracker@claude/intelligent-curie-j1hsfo`, and reconcile later. (Michael: cleaner alternative — copy the findings file into the local project before starting, so the hourly auto-sync carries it into `trading-src`.)

## Fixed context — do not re-check, do not re-litigate

- Gate "Phase 1 exit backtest complete": **SATISFIED**. Item 4b closed 2026-05-05; Mode 1 beat Mode 3, PF 1.272 vs 1.078; winner locked `exit_mode='single'`. R1 is administratively clear to run.
- Do NOT conflate with **Item 12 / Profile E**: ATR as volatility *scaler* is a different question from ATR as signal *gate*. R1 is the gate question only.
- `stock-tracker` (the deployed Flask dashboard, `app.py`) is settled `[Proven]`: it has never contained `swing_trader.py`, any ATR, or any signal gate — verified twice by full git forensics on 2026-07-02. Do not spend time re-checking it. Reference: `TECH_ANAL_USAGE_AUDIT_2026-07-02.md` in that repo.

## History (why this handoff exists)

1. **2026-07-02, session #1** (branch `claude/tech-anal-vs-atr-gate-4xlygg`): verify-first FAILED — wrong repo. Located the real target: private repo **`trading-src`** (135 files incl. `swing_trader.py`, `leap_strategy.py`, planning docs, `MASTER_STATUS_BOARD.md`; hourly auto-sync from the local machine).
2. **2026-07-02, session #2** (branch `claude/intelligent-curie-j1hsfo`): pointed at `stock-tracker` again; verify-first FAILED again, independently re-confirmed; appended the second record to the findings file.
3. **This handoff**: for session #3, which must be started scoped to `trading-src`.
